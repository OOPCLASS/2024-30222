package ro.library.service;

import ro.library.model.Comparable;

public class SortingService {

  public void sort(Comparable[] objects) {
    // TODO implement a sorting algorithm
    // if (objects[i].compareTo(objects[i + 1]))
  }
}
